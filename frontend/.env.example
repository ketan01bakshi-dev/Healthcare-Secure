NEXT_PUBLIC_API_BASE_URL=http://localhost:8000

# Phone / Capacitor (same Wi-Fi as this PC):
# 1. Find your PC LAN IP (e.g. ipconfig → IPv4).
# 2. Set NEXT_PUBLIC_API_BASE_URL=http://YOUR_LAN_IP:8000
# 3. Rebuild: npm run mobile:build  (env is baked into the static export)
