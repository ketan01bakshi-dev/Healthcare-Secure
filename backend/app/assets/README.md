# Prescription template assets
#
# Drop your branding files here (optional):
#   doctor_seal.png     — circular stamp / signature seal (PNG/JPG, ~300x300+)
#   letterhead.png      — A4 letterhead background (2480x3508 @ 300dpi or similar)
#
# Or set absolute paths in backend/.env:
#   DOCTOR_SEAL_PATH=D:\path\to\seal.png
#   PRESCRIPTION_LETTERHEAD_PATH=D:\path\to\letterhead.png
#
# If missing, the PDF generator draws a built-in clinic header + circular seal.
